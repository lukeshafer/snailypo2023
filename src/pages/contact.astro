---
import Page from "../layouts/Page.astro";
import { links } from "../config";

const { title, bg } = links.contact;
---

<Page title={title} bg={bg}>
	<h1 class="lowercase text-3xl">Contact</h1>
</Page>
